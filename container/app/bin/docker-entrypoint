#!/bin/bash

export PATH="${PATH}:/opt/lantern/bin:/opt/lantern/service"

system-config

# systemd does not run correctly inside docker, so start manually:
http &
lora &

echo "entering shell..."
zsh

trap : TERM INT; sleep infinity & wait